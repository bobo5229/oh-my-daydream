---
title: 写作边界
type: meta
tags:
  - daydreamer/meta
updated: 2026-09-06
---

# 写作边界

本文定死 **写什么、不能写什么**。规则总路由与全局注意见 `AGENTS.md`。怎么写见 `style.md`。具体用哪个词见 `wording.md`。这一节写什么、写到哪见该故事的 `beats.md`。不维护独立雷区文档。

## 安全底线（不可试）

- 性相关角色均为 **18 岁以上**；年龄含糊按成人处理，但须在设定里写清成年
- 禁止未成年及任何将角色性化为未成年的写法（含暗示、包装）
- 禁止真实在世人物的露骨性写、现实犯罪操作教学

以上为硬边界，优先于一切节拍与偏好。

## 可写范围

- 成年角色的虚构 NSFW / 情色 / 露骨描写
- 尺度随节拍：温存、直白、狠场、羞辱等均可，由 `beats.md` 与场景决定
- 未在 `wording.md` 硬禁、也未在当次节拍里加禁的，视为可写；助手不自行加码封杀

## 职责分工

完整路由表见 `AGENTS.md`。本文只负责安全与可写范围；文风 → `style.md`；用词 → `wording.md`；流程 → `sop-1.md` / `sop-2.md`。

不维护 `taboos.md` / 雷区：路数与是否展开某玩法，由节拍决定；需要临时加禁时写在当次 `beats.md`。

## 协作

- 流程以 `sop-1.md` / `sop-2.md` 为准（选用哪套由当次指定）
- 未同意不写正文
- 节拍未要求的玩法、人物、转折不擅自补全
- 故事级加禁只做加法（多禁），不能把 `wording.md` 里的硬禁改回可写
